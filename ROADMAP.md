# FEUILLE DE ROUTE DE DÉVELOPPEMENT (ROADMAP)

## Phase 1 : Socle Technique & Infrastructure
- [x] **Étape 1.1** : Initialisation du projet Next.js (App Router), TypeScript et Tailwind CSS.
- [x] **Étape 1.2** : Configuration de Supabase (Client, middleware Auth et variables `.env.local`).
- [x] **Étape 1.3** : Configuration de `styles/globals.css` avec les Design Tokens du thème "Éclat Minéral" et injection des composants de base `shadcn/ui` (Button, Input, Accordion, Dialog).

---

## Phase 2 : Frame Global (Front-Office)
- [x] **Étape 2.1** : Création du composant `Header` fixe (Nom du photographe, barre de navigation, bouton de connexion).
- [x] **Étape 2.2** : Création du composant `Footer` fixe (Réseaux sociaux, mentions légales, copyright).
- [x] **Étape 2.3** : Création du Canvas central vierge reliant le Header et le Footer sur la Page d'Accueil (`/`).

---

## Phase 3 : Back-Office - Créateur de Pages (Page Builder)
- [x] **Étape 3.1** : Métadonnées de Page (Formulaire de création de page : Titre H1/SEO, MenuTitle, Slug URL, statut publié/brouillon).
- [x] **Étape 3.2** : Conteneur Drag & Drop des modules (`@hello-pangea/dnd`) & Menu déroulant « + Ajouter une section ».
- [x] **Étape 3.3** : Composant Accordéon Compact (Drag handle, nom du module, Toggle Eye de masquage, suppression).
- [x] **Étape 3.4** : Formulaire CRUD en vue dépliée (Édition des champs textuels, boutons CTA, ancres `#id` et médias).

---

## Phase 4 : Back-Office - Gestionnaire de Menu & Navigation
- [x] **Étape 4.1** : Écran « Navigation & Menus » (Interface d'organisation de l'arborescence du Header/Footer).
- [x] **Étape 4.2** : Rattachement dynamique des pages (Mise en lien automatique du `MenuTitle` des pages créées dans la navigation).
- [x] **Étape 4.3** : Re-structuration par Drag & Drop du Menu (Gestion des éléments de Niveau 1, sous-menus de Niveau 2, et liens vers ancres/externes).
- [x] **Étape 4.4** : Injection des Presets Onboarding de Navigation (Artiste, Commercial, Passionné).
- [x] **Étape 4.5** : Rendu Front-Office dynamique du Header & Footer (branchage sur le store Navigation partagé, sous-menus Niveau 2, ancres/URL externes).

---

## Phase 5 : Intégration BDD & Persistance (Supabase / Drizzle ORM)
- [x] **Étape 5.1** : Schéma BDD PostgreSQL via Drizzle ORM (tables `profiles`, `pages`, `page_modules`, `navigation_entries` avec auto-jointure Niveau 2), migrations & seed initial (app Next intacte).
- [x] **Étape 5.2** : Hydratation SSR des stores depuis la BDD (repository layer, prop `initialData` sur les Providers, fallback seed, contrats UI préservés).
- [x] **Étape 5.3** : Persistance CRUD des actions (repositories d'écriture, Route Handlers `/api/*` validés zod, synchro persistée Pages ↔ Navigation, fallback hors-BDD).
- [x] **Étape 5.4** : Auth Supabase & RLS (`@supabase/ssr`, `/admin/login`, middleware `/admin/*`, politiques Owner RW/Public RO + trigger `profiles.id = auth.uid()`, fallback démo).

---

## Phase 6 : Stockage Médias & Performance (Supabase Storage / Galerie)
- [x] **Étape 6.1** : Supabase Storage `portfolio-media`, Media Library `/admin/media` (upload/EXIF/blur, suppression), table `media`, wrapper `next/image` + MediaPicker branché sur les modules (fallback démo).
- [x] **Étape 6.2** : Rendu public optimisé des galeries (renderers `src/components/modules/`, `MediaImage` lazy/priority/blur, Lightbox EXIF, page `/demo`).
- [x] **Étape 6.3** : Pages publiques dynamiques `/[slug]` + Accueil `/` (getPublicPage BDD→seed, `PageModuleRenderer`, `generateMetadata` SEO/OG) & migration proxy Next 16.

---

## Phase 11 : Rubrique « Galeries & Portfolio » (modules multi-variantes)
- [x] **Étape 11.1** : Domaine & persistance — famille `gallery` à variantes (`static` / `dynamic` / `portfolio`) discriminées dans le JSONB (**sans migration** : `module_type` reste `gallery`), effets, ombre, bordure, CTA, Lightbox, albums, `resolveGalleryContent` rétro-compatible (legacy masonry → `static`), catalogue à 3 cartes, `galleryContentSchema` zod.
- [x] **Étape 11.2** : Effets & briques visuelles — `src/lib/gallery-effects.ts` (Passe-partout de Musée / Sous-Verre / Polaroid papier glacé, light/normal/strong), ombre nacre 4 niveaux, bordure (épaisseur + couleur), `CTAButton`.
- [x] **Étape 11.3** : Grille & vignettes — `GalleryGrid` (uniforme / masonry, colonnes responsives), `GalleryItem` (`MediaImage` lazy, ratio anti-CLS, effets, survol, curseurs selon variante), `GalleryAlbumBadge`.
- [x] **Étape 11.4** : `LightboxModal` générique (unique) — clavier/Échap, focus trap, ARIA, zoom HD, plein écran, cycle fit → 1,5× → 2,5×, pan au clic maintenu sans scroll.
- [x] **Étape 11.5** : `GalleryManager` — orchestration static / dynamic / portfolio et branchement du rendu public (`PageModuleRenderer`).
- [x] **Étape 11.6** : Éditeurs Back-Office — `ImportMediaPanel`, `GalleryLayoutPanel`, `EffectSettingsPanel`, `GalleryCtaPanel`, `LightboxSettingsPanel`, `AlbumManagerPanel`, `GalleryImagesPanel`, routeur `ModuleGalleryEditor`.
- [x] **Étape 11.7** : Démo `/demo` (3 galeries + albums) & validation `tsc` / `eslint` / `build`.
- [x] **Étape 11.8** : Suivi `ROADMAP.md` + `CHANGELOG.md`.

### Correctifs après recette (Phase 11)
- [x] **Étape 11.9** : **Gallery Dynamic en clic simple** (garde anti-fermeture du backdrop) ; **alerte** dans l'éditeur quand le CTA est activé sans libellé ou sans lien.
- [x] **Étape 11.10** : `LightboxModal` — **pan fiable** (armement du drag avant capture, écouteurs fenêtre, `translate3d`, `overscroll` contenu, aucun ascenseur natif) et **préchargement** des images voisines (fluidité du diaporama).
- [x] **Étape 11.11** : Focus — restauration **uniquement** pour les ouvertures clavier (suppression de l'anneau coloré résiduel) et anneau de focus discret.
- [x] **Étape 11.12** : Galeries **vides par défaut** (photos et albums ajoutés dynamiquement, aucun masque pré-créé) ; seed nettoyé de la galerie vide.
- [x] **Étape 11.13** : **Conversion WebP qualité 80 à l'upload** (`sharp`, JPEG/PNG/WebP/AVIF ; SVG/GIF/vidéos exclus ; repli sur l'original) — EXIF lu sur l'original, dimensions/blur sur le WebP.
- [x] **Étape 11.14** : **Lazy loading** systématique des photos de galerie et prop `quality` sur `MediaImage` (80 en Lightbox et grilles).
- [x] **Étape 11.15** : Validation `tsc` / `eslint` / `build` + `ROADMAP.md` / `CHANGELOG.md`.
- [x] **Étape 11.16** : **UX Portfolio & CTA** (plan [`plans/ROADMAP-11.16-galleries-albums-cta-linkpicker.md`](plans/ROADMAP-11.16-galleries-albums-cta-linkpicker.md)) — vocabulaire **« Album »** généralisé (fin de la collision avec « thème » = identité visuelle, alignement sur le code) et **sélecteur de cible du CTA** : deux menus (« Aller vers une page du site » / « Aller vers une section de page ») + lien libre conservé, mode **dérivé de `href`** (zéro migration), aperçu en clair de la destination, et `CTAButton` routé via `NavLink` (défilement lissé sous Header fixe). Ajout du réglage **« Affichage sur les couvertures »** (permanent / au survol de la photo / aucun, `badge.display` optionnel, **sans migration**) avec **repli tactile automatique** — sur mobile le texte reste visible, le doigt ne pouvant pas survoler.

- [x] **Étape 11.17** : **Ergonomie des éditeurs de modules** (plan [`plans/ROADMAP-11.17-editor-zones-ux.md`](plans/ROADMAP-11.17-editor-zones-ux.md)) — le formulaire d'un module empilait quatre **échelles** différentes (le bloc, la galerie, les albums, la photo) sans repère de niveau, d'où sept remarques d'utilisateur toutes formulées « de quoi ? ». Trois lots : **(A)** libellés non techniques (fin de `Masonry`, `Light/Medium/Strong`, `Gallery Static/Dynamic/Portfolio` ; « Titre d'affichage » → « Nom de la section dans le back-office » dont l'aide était **fausse** ; EXIF → « réglages de l'appareil photo » ; en-tête d'album nommé) ; **(B)** réglages techniques déplacés dans une zone **« Réglages avancés » repliée** en fin de formulaire et **suppression d'un doublon réel** (« Animation au survol » était exposée deux fois, dans `ModuleSettingsForm` et `GalleryLayoutPanel`) ; **(C)** composant partagé **`EditorZone`** (titre qui nomme la cible + phrase de portée obligatoire + teinte d'accent, repli sans dépendance nouvelle) et refonte du panneau Galerie en **4 zones** avec **barre d'ancres** collante ; **(D)** généralisation aux **9 autres éditeurs** (Héro ×4, À propos, Services, FAQ, Contact, Bandeau CTA). **Onglets écartés** (le formulaire est déjà dans un accordéon, et les onglets cacheraient les valeurs par défaut) au profit de zones + ancres. **Aucun champ perdu, aucun changement de schéma, rendu public strictement inchangé.**

- [x] **Étape 11.18** : **Correctif Héro vidéo — fin du « flash » de la photo de secours** [`HeroVideoBackground`](src/components/modules/hero/HeroVideoBackground.tsx). Deux défauts cumulés : `isDesktop` valait **`true` par défaut**, donc le HTML SSR et le premier rendu client montaient la branche **desktop** même sur téléphone (on voyait brièvement la photo de secours *ordinateur* avant la bascule) ; et la photo de secours desktop était affichée **deux fois** (`poster` de `<video>` **et** `<img>` superposé), soit deux images lourdes en concurrence avec la vidéo. Correctifs : état initial **indécis** (`null`) → fond anthracite neutre jusqu'à résolution des media queries, **un seul** mécanisme d'image, **fondu de sortie** au démarrage réel de la vidéo (`onPlaying`), `preload="auto"`, et conseil d'éditeur invitant à choisir une image **extraite de la vidéo**. **Rendu du site inchangé par ailleurs.**

- [x] **Étape 11.19** : **Héro vidéo — seconde passe, suppression définitive de l'« image fantôme »** (constat de recette : *« le problème existe toujours, mais maintenant avec en plus un fond noir qui apparaît un bref instant »*). La 11.18 conservait le rôle de **poster de chargement** à la photo de secours desktop — or une image décodée avant le premier plan vidéo **est** montrée, aucun fondu ne peut l'éviter ; et son état initial « indécis » (`null`) ajoutait une frame au fond noir. Nouvelle répartition **un média = un seul rôle** : `videoUrl` = habillage animé, `fallbackMobile` = remplace la vidéo sur téléphone, `posterDesktop` = **repli si la vidéo ne peut pas être lue** (jamais pendant le chargement ; champ renommé « **Image de repli — ordinateur** »). Pendant le téléchargement, fond **anthracite** au ton de l'overlay puis **vidéo révélée en fondu** dès la première image décodée (`onLoadedData`, qui couvre aussi un autoplay bloqué). Les images de repli sont choisies **en CSS** (`md:hidden` / `hidden md:block`) : présentes dès le HTML du serveur, **sans frame indécise**. Résultat attendu : **une seule transition** au chargement, plus aucune image parasite.

- [x] **Étape 11.20** : **Gestion des albums du Portfolio par grille de vignettes** (plan [`plans/ROADMAP-11.20-albums-thumbnail-grid.md`](plans/ROADMAP-11.20-albums-thumbnail-grid.md)) — chaque album était rendu **intégralement déplié** : la hauteur du panneau croissait en *albums × photos* (trente albums de vingt photos = **six cents vignettes** montées dans le DOM), ce qui rendait la création de plusieurs dizaines d'albums impraticable. Cinq lots : **(A)** champ **`hidden`** optionnel sur l'album (`z.boolean().optional()`, repli `false` ⇒ les contenus antérieurs restent visibles, **aucune migration BDD**), branché sur les **trois** consommateurs publics — grille portfolio, **SEO/Open Graph** (`galleryImageSources`) et `PublicModules` ; **(B)** **grille de vignettes** ([`AlbumThumbnail`](src/components/backoffice/pages/modules/gallery/AlbumThumbnail.tsx) : couverture lazy, **numéro d'ordre**, compte de photos, actions **au survol et au focus clavier**, badge « Masqué », signalement « Aucune photo — invisible sur le site ») et [`AlbumGrid`](src/components/backoffice/pages/modules/gallery/AlbumGrid.tsx) (tuile « Nouvel album », grille 2/3/4 colonnes, réordonnancement **↑/↓ accessible au clavier**) ; **(C)** **vue d'album en place** (pleine largeur, sans route ni persistance supplémentaire, réutilisation de `GalleryImagesPanel`, garde-fou si l'album disparaît) ; **(E)** **import groupé** ([`AlbumFolderImportPanel`](src/components/backoffice/pages/modules/gallery/AlbumFolderImportPanel.tsx) : un **dossier parent → un album par sous-dossier** via `webkitRelativePath`, analyse du dossier **avant** envoi, photos à la racine regroupées au nom du dossier, sous-dossier sans image **ignoré et signalé**, fichiers refusés comptés **par dossier**, confirmation explicite au-delà du lot conseillé de 50 photos, aucun album vide créé) ; **(D)** glisser-déposer sur la grille, **subordonné à un prototype** (cf. §5.1 du plan) et **jamais seul moyen de réordonner** (les ↑/↓ subsistent). Sont restés des réglages de la **galerie** (et non de l'album) : disposition, effets, ombre, bordure, badge, diaporama et CTA — l'album est une **unité de contenu**.
- [x] **Étape 11.20.a** : **Séparation visuelle de la vue d'album** (constat de recette en deux passes : *« tout ce qui est au-dessus du bouton est associé aux paramètres de l'album, car relié par la même couleur de barre »*, puis *« la barre au-dessus du bouton n'a pas changé de couleur »*). **Cause racine** : la vue d'album était rendue **dans** la `EditorZone` « Les albums », dont la barre d'accent (`--zone-content`) court sur toute la hauteur — un bandeau **imbriqué** de couleur différente ne corrige rien (la barre de la zone continue, et le bandeau, en retrait du `p-3`, fait apparaître une **seconde** barre parallèle). **Correctif** : `editingAlbumId` **remonté** à `ModuleGalleryEditor`, vue d'album extraite dans `AlbumEditorPanel` et rendue dans une `EditorZone` **sœur** de teinte **`detail`** (vert d'eau, distincte du bleu glacier) — **la barre change donc réellement de couleur au-dessus du bouton « Retour vers la galerie des Albums »**, sans jeton CSS ajouté. La grille cède la place pendant l'édition (aucune route ni persistance supplémentaire : D-4 préservée).
- [x] **Étape 11.20.b** : **Libellés du réglage de grille + explication portée par chaque option** (constat de recette : *« type de grille / grilles régulières / mosaïque me semblaient difficiles à comprendre »*). Le champ « Type de grille » devient « **Format d'affichage** », et « Grille régulière » / « Mosaïque (hauteurs libres) » deviennent « **Toutes au même format** » / « **Chacune à son format** » — on décrit le **résultat** au lieu de nommer la technique. Nouveauté d'interface **réutilisable** : `SelectItem` / `SelectField` acceptent une **explication par option**, rendue **hors de `ItemText`** pour que le champ fermé n'affiche que le libellé (Radix recopie `ItemText` dans le déclencheur) ; l'aide sous le champ disparaît, remplacée par un tooltip « i » sur ce que le réglage ne fait pas. **Valeurs stockées, calcul de grille et rendu public strictement inchangés.** - [x] **Étape 11.20.c** : **Réorganisation de la zone « Apparence » + correctif de l'arrondi avec encadrement.** Zone renommée « **Apparence de la galerie d'Albums** » (Portfolio) et structurée en **deux sous-titres** : « **Disposition des couvertures des albums dans la galerie Portfolio** » (colonnes, écarts, format d'affichage) puis « **Format des couvertures des albums** » (**Effet de finition → Arrondi → Encadrement → Ombre portée → Effets au survol → Infos en pied de couverture**). Trois déplacements : l'**arrondi** quitte « Disposition de la grille » pour rejoindre l'encadrement (c'est cette proximité qui manquait pour juger leur combinaison) ; le bloc « Affichage sur les couvertures » quitte la zone 1 pour le sous-titre « Format » (nouveau `AlbumCoverBadgePanel` — il décrit l'aspect, pas le contenu) ; « Effets au survol » se place après « Ombre portée ». **Correctif technique** : l'image intérieure recevait le **même rayon** que le cadre alors qu'elle est enchâssée de l'encadrement **et** de la marge de l'effet — d'où deux arcs décalés dans chaque coin et l'impression d'incompatibilité. `galleryConcentricRadius()` applique désormais `max(0, rayon − épaisseur enchâssée)` ; arrondi, encadrement, ombre et effet restent **cumulables** (aucun réglage rendu exclusif). **Vocabulaire selon la variante** (`galleryItemWording`) : « couvertures d'albums » en Portfolio, « photos » sur static / dynamic. **Aucune donnée, aucun schéma, aucun rendu public modifiés.** *Reste ouvert : les chaînes publiques `alt` / `aria-label` de `GalleryGrid` qui disent encore « photo » pour une couverture d'album.*
- [x] **Étape 11.21** : **Échelle typographique des éditeurs de modules** (constat de recette : *« il est difficile de distinguer les titres des sous-titres et des libellés… conséquence catastrophique pour le UX »*). Le constat était **mesurable** : quatre rôles partageaient **12 px**, le **sous-titre de sous-zone et le libellé de champ étaient rigoureusement identiques** (taille, graisse ET couleur), et le **titre de zone (13 px) était plus petit que le texte saisi** (14 px bureau / 16 px mobile) — hiérarchie inversée. Correctif : **échelle unique** dans `editor-type.ts` — N0 nom de module 15/600 (déjà en place dans `ModuleRow`), N1 titre de zone **14/600**, N2 portée 12/400 gris, N3 sous-titre **13/600**, N4 libellé **12/600**, N5 aide **11/400**, ML micro-libellé 11/500 capitales, **contenu saisi inchangé (16 px mobile : anti-zoom iOS, seule exception assumée)**. Chaque niveau se distingue par **au moins deux critères** (taille + graisse + couleur), un écart d'un pixel ne se percevant pas. **Second marqueur : la géométrie** — contenu de zone et de sous-zone indentés (`EDITOR_INDENT`), la taille étant un signal fragile, l'indentation non. **Trois composants partagés suffisent à corriger les dix éditeurs** (`EditorZone`, `form-fields`, `gallery/fields`), et le **cinquième système** (`uppercase tracking-wider` muté) est rattaché à l'échelle : N3 pour les titres structurels, ML pour les compteurs. Aucune donnée, aucun comportement, aucun rendu public modifiés.
- [x] **Étape 11.22** : **Police unique du Back-Office, indépendante du thème du site public.** Le layout racine chargeait le duo éditorial du thème sur `<html>` et [`globals.css`](src/app/globals.css) l'appliquait globalement : le back-office héritait de Plus Jakarta **plus l'interlettrage éditorial**, et surtout la règle **globale** `h1…h6 { font-family: var(--font-heading) }` imposait **Cormorant Garamond (serif)** à tous ses titres — y compris les niveaux N1/N3 de l'échelle 11.21, réglés en sans-serif semibold : la hiérarchie de 11.21 était **partiellement annulée**. Correctif : **Inter** déclarée via `next/font` (`--font-admin`, police **variable** = un seul fichier pour toutes les graisses, x-hauteur élevée, chiffres tabulaires) devient la police **par défaut de l'application** ; le duo éditorial est **restreint au site public** via `body:not(:has(.admin))` et `:where(...) :is(h1…h6)` — le `:where()` **préserve la spécificité d'origine (0,0,1)**, donc aucun risque sur les graisses des titres publics ; `--font-sans` suit la police d'application. **Point technique décisif** : la police est posée sur `<body>` et **non** sur `.admin`, car les **portails Radix** (dialogues, menus, feuilles) sont montés dans `<body>`, hors de `.admin` — c'est ce qui rend l'application cohérente **sans modifier un seul composant**. Restent volontairement : les **aperçus** de `VisualIdentityScreen` en serif (ils simulent le site public) et `font-mono` (pile système) pour les valeurs techniques. Réserve : sans `:has()`, le site public s'afficherait en police d'application — dégradation visuelle seulement. *Observation reportée : les jetons de palette de `.admin` ne couvrent pas non plus les portails (défaut préexistant, même nature).*
- [x] **Étape 11.23** : **Module « Galerie Portfolio » — libellés, dédoublonnage du parcours d'import, famille d'effets de survol cumulables.** (1) *Libellés* : le titre de zone devient « **Les albums de la Galerie Portfolio** » et le compteur « **Nombre d’Albums actuellement créés : N** » — N étant le nombre réel d'albums ; un niveau d'**annotation en phrase** (ML-b) est ajouté au barème, les capitales du micro-libellé ML convenant mal à une phrase. (2) *Dédoublonnage* : le bloc encadré « Importer un dossier d'albums » et son bouton sont **retirés**, le composant `AlbumFolderImportPanel` est **supprimé du projet**, la prop devenue inutile `demo` est retirée et la phrase de cadrage « Un dossier importé devient un album » est **réécrite** (elle décrivait une fonctionnalité disparue) — vérification : **aucun orphelin** (référence, style ou gestionnaire). Le parcours conservé est la tuile « + Nouvel album », et l'import de **photos** dans un album reste disponible. (3) *Effets de survol* : nouvelle famille **optionnelle et cumulable** — **Zoom** et **Élévation** désormais **réglables finement** (ils étaient codés en dur), plus **Parallaxe**, **Brillance**, **Saturation et contraste**, **Bordure lumineuse** ; persistée dans `layout.hoverEffects` du JSONB, **optionnelle** dans le schéma zod avec **repli champ par champ** (aucune migration, rendu existant préservé : zoom 105 % / élévation 4 px). Uniquement `transform` / `filter` / `box-shadow` (GPU, aucun recalcul de mise en page) ; déclenchement sur **survol réel** et **focus clavier**, **suppression** — et non accélération — sous `prefers-reduced-motion`. Valeurs transmises en **variables CSS** depuis `galleryHoverCssVars()`, mécanique dans `globals.css`. (4) *Lisibilité* : « Voile dégradé au survol » → « **Accentuation de la lisibilité** », avec la description demandée ; l'effet **correspond** bien à cette description (assombrissement du bas vers le haut) et a été **découplé** de l'interrupteur général pour se cumuler librement.
- [x] **Étape 11.24** : **Désambiguïsation des deux boutons d'aperçu du Back-Office** (constat de recette : *« cela me semble être un doublon, perturbant pour le UX »*). Diagnostic : les deux commandes sont **distinctes** — « Aperçu du site » dans la barre du tableau de bord ouvre l'**accueil** (`/`), « Aperçu » dans l'en-tête de la page éditée ouvre **cette page** — mais **trois facteurs les faisaient percevoir comme un doublon** : des libellés quasi identiques, la **même position** (colonne de droite de deux barres superposées, même icône `ExternalLink`, même nouvel onglet), et une identité réelle lorsque la page éditée *est* l'accueil. **Décision du propriétaire : conserver les deux** (chacune a son utilité — l'aperçu contextuel montre ce qu'on édite, l'aperçu global est le seul recours sur les écrans sans aperçu), et lever l'ambiguïté par le **libellé** : le bouton contextuel devient « **Aperçu de la page** ». Une variante plus structurelle — déplacer l'aperçu global dans la barre latérale, hors de la zone d'actions du compte où il voisine avec « Déconnexion » et l'avatar — a été **proposée puis écartée**. Le commentaire d'en-tête de `PageEditor.tsx` documente désormais pourquoi les deux libellés sont distincts.
- [x] **Étape 11.25** : **Indicateur de page d'accueil dans la liste des pages.** Avant, la maison n'apparaissait **que sur les pages non-accueil** — la page d'accueil portait un badge « Accueil » et **aucune maison** : rien ne montrait donc à la couleur qui était l'accueil, et la ligne concernée **décalait ses icônes** d'un cran. Désormais la maison est présente **sur chaque ligne** et c'est sa **couleur** qui informe : **grise et cliquable** (cette page n'est pas l'accueil ; le clic la désigne via `setHomePage`), **noire et non interactive** (c'est l'accueil), avec le tooltip « **Ceci est votre page d'accueil** » sur celle-ci et « Définir cette page comme page d'accueil » sur les autres. **Deux pièges évités** : `disabled` était **inutilisable** (le `disabled:pointer-events-none` de `ui/button.tsx` empêche tout survol, donc **le tooltip ne se serait jamais affiché**) — l'état « accueil » est rendu par un indicateur `role="img"` avec `title` **et** `aria-label` ; et un bouton désactivé mais focusable serait resté un **piège au clavier** (un indicateur n'est pas une action, il sort de l'ordre de tabulation). La couleur n'étant pas le seul signal, le **badge « Accueil »** est conservé comme pendant textuel, avec l'URL canonique `/`.

- [x] **Étape 11.26** : **Sélecteur de lien généralisé à tous les CTA** (plan [`plans/ROADMAP-11.21-cta-link-picker-generalise.md`](plans/ROADMAP-11.21-cta-link-picker-generalise.md) — le numéro du **fichier** de plan précède l'attribution de l'étape 11.21 à l'échelle typographique ; l'étape est donc enregistrée en 11.26). Cinq éditeurs (Bandeau CTA, Héro, Slider, Vidéo, Parallaxe) demandaient encore de **taper un slug à la main** (`/portfolio`) alors que le sélecteur de cible existait depuis 11.16 mais n'était branché que sur le CTA de galerie. Trois lots : **(A)** découpage en **un hook + deux vues** — `useLinkTargetIndex()` construit l'index **une seule fois** par éditeur (le slider le passe à ses N diapositives : N parcours complets supprimés, défaut B de l'audit), `LinkTargetField` conserve sa **signature publique inchangée** (rétrocompatibilité du CTA de galerie) et délègue le regroupement des sections au nouveau `groupAnchorTargets()`, tandis que `LinkTargetSelect` apporte la **vue compacte** (un seul menu : pages + sections de la page courante + autres sections + « Autre lien (externe)… » révélant le champ libre et l'aperçu) ; **(B)** migration des cinq éditeurs — **vue complète** quand il n'y a qu'un bouton (Bandeau, Héro), **vue compacte** dès qu'il y en a plusieurs (Slider, Vidéo, Parallaxe), le `tip` « Page du site (/slug) » devenu inutile est supprimé ; **(C)** correctif d'un **défaut latent du rendu public** : `HeroTextBlock` et `PublicModules` rendaient leurs CTA par un simple `<a href>`, si bien qu'une ancre saisie aurait sauté **sous le Header fixe** — les deux adoptent la discrimination déjà écrite dans `CTAButton` (`https://` → nouvel onglet, `mailto:`/`tel:` → même onglet, **interne/ancre → `NavLink`** et son défilement compensé). **Le mode reste dérivé de `href`** (aucun champ `targetMode` persisté), **aucune migration** et aucun champ ajouté au schéma ; écart **assumé** avec la demande initiale (deux menus en cascade) : le menu **groupé** est conservé, à re-tester en recette (11.21-D6).

- [x] **Étape 11.27** : **« Bandeau message ou d'appel à l'action »** (plan [`plans/ROADMAP-11.27-bandeau-message-cta.md`](plans/ROADMAP-11.27-bandeau-message-cta.md)). Le module `cta-banner` n'était qu'un encart coloré (`max-w-7xl`, `bg-accent/30`, texte centré) : il devient un **séparateur éditorial pleine largeur** offrant **quatre fonds** — couleur unie (jeton du thème **ou** palette/pipette), **carrousel** de photos (flèches, puces, glissement ou fondu, autoplay, swipe), photo en **parallaxe** avec **position verticale** réglable, **vidéo** — et **trois hauteurs** (≈ 1/3, 1/2, 3/4 de l'espace compris entre le bas du Header et le bas de la fenêtre), avec CTA optionnel. **Réutilisation plutôt que duplication** : `BaseHero` sert de cadre (trois props **optionnelles** ajoutées sans changer le rendu des Héro — `minHeightClass`, `pullUp`, `titleTag`), `HeroTextBlock` porte le message, rendu en **`h2`** puisqu'un séparateur ne doit pas créer un second `<h1>` sur la page, et les calques `HeroStaticBackground` / `HeroParallaxBackground` / `HeroVideoBackground` fournissent les fonds — avec une nouvelle prop **`focalY`** (`object-position`) appliquée à l'image animée **et** à son repli mobile. **Le moteur du carrousel est extrait** dans `src/components/modules/shared/` (`useSliderEngine` + `SliderControls`) et consommé par le Héro **et** par le bandeau : le code est **déplacé**, le comportement du Héro est inchangé, et `shared/` (namespace neutre) évite toute importation croisée entre modules au sens du `.kilorules`. **Pipette et palette mutualisées** : le nouveau `ColorField` — extrait de l'écran d'identité visuelle, qui l'utilise désormais — réunit sélecteur natif, saisie libre, palette maison et **API `EyeDropper`** (avec son message de repli). **Constantes nommées** : `--header-height` et utilitaires `.banner-h-small/standard/large` dans `globals.css` (repli `vh` puis `svh`, mécanique en CSS comme pour les effets de galerie) — l'éditeur ne transmet qu'un **mot** (« Petit / Standard / Grand »), jamais un pourcentage. **Aide intégrée** en tête d'éditeur (usages : message d'appel à l'action, slogan, séparateur éditorial) et description du catalogue réécrite. **Défauts demandés** : la fabrique `createCtaBannerContent()` produit **parallaxe + hauteur standard + CTA activé** (carte du catalogue et seed de l'Accueil), tandis que le **résolveur** retombe sur le fond **couleur** pour un contenu enregistré avant cette étape — un bandeau ancien n'invente donc aucune image à télécharger ; `heading` / `subheading` restent la **source de vérité** du message, projetée sur `titleH1` / `subtitleH2` (surface lue par `BaseHero`), ce qui laisse le SEO de partage intact. **Aucune migration BDD**, aucun champ de schéma ajouté ; contrôles `npx tsc --noEmit`, `npm run lint` et `npm run build`.

> Note : les Phases 7 (Héro 7.1→7.4), 8 (Profil propriétaire), 9 (Identité visuelle) et 10 (Onboarding) sont documentées dans `plans/ROADMAP-*.md` et restent à reporter dans cette feuille de route.