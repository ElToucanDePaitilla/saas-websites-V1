import { TopBannerScreen } from "@/components/backoffice/top-banner/TopBannerScreen";

/**
 * Route `/admin/bandeau-alerte` — réglage global « Mini-bandeau Alerte / Promo ».
 * Écran client : activation, message, mode d’affichage, typographie, couleurs,
 * dimensions et gaps, lien, aperçu en direct.
 */
export default function AdminTopBannerPage() {
  return <TopBannerScreen />;
}
