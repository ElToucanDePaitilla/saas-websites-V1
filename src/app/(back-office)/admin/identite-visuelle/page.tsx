import { VisualIdentityScreen } from "@/components/backoffice/visual-identity/VisualIdentityScreen";

/**
 * Route `/admin/identite-visuelle` — module « Identité visuelle / Logo » (9.1).
 * Écran client : mode Texte | Logo, styles, upload logo, aperçu en direct.
 */
export default function AdminVisualIdentityPage() {
  return <VisualIdentityScreen />;
}
